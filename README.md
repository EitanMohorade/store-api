# backend-dmtoolkit-api

## Spring Boot Project

Quick start:

- Run: `mvn -q spring-boot:run` or `java -jar target/store-api-0.0.1-SNAPSHOT.jar`

A sample REST endpoint is available at `http://localhost:8080/api/products`.